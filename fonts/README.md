# Required fonts

Place the licensed Microsoft font files here without committing them:

- `Consolas-Regular.ttf` — Consolas Regular
- `microsoft-jhenghei.ttf` — Microsoft JhengHei Regular

The benchmark validates the internal family name and every required glyph.
